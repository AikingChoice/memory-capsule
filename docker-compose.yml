version: "3.8"

services:
  memory-capsule:
    build: .
    container_name: memory-capsule
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped
